# 📋 Lista de Tarefas AJAX — Prática Aula 38
CRUD completo com fetch + loading + debounce + DOM parcial.

## npm install && npm run dev

## 12 TODOs
1-5. apiRoutes.ts — GET (com busca), POST, PUT, PUT/toggle, DELETE
6. carregarTarefas() — fetch GET + loading state (finally)
7. exibirTarefas() — template literals com id único para DOM parcial
8. criarTarefa() — fetch POST + botão disabled
9. editarTarefa() — fetch PUT + DOM parcial (só textContent)
10. toggleConcluida() — fetch PUT toggle + DOM parcial (classe + emoji)
11. removerTarefa() — fetch DELETE + confirm
12. buscarComDebounce() — clearTimeout + setTimeout(300ms)
